require('lualine').setup {
  options = {
    icons_enabled = true,
    -- theme = custom_auto,
    theme = 'tokyonight',
    always_divide_middle = true,
    -- section_separators = '',
    -- component_separators = '',
    globalstatus = true,
 }
}

